import React from 'react'
import styled from 'styled-components'

const PDFViewer = ({ manuscript, pdfFile, history }) => {
  return <Root>aici va fi pdf</Root>
}

export default PDFViewer

// #region styles
const Root = styled.div`
  align-items: center;
  flex: 1;
  display: flex;
  flex-direction: column;
  overflow: scroll;
`
// #endregion
